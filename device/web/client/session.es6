import React from 'react';

class Session extends React.Component {
    render(props) {
        return (
            <div>
                This is the session detail screen.
            </div>
        );
    }
}

export default Session;